# NoteSail
Welcome to NoteSail! This web application uses Python Django to create a note-taking app that also, in itself, has a homepage to introduce the app 
before the user "decides to use it." Styling is the name of the game.
